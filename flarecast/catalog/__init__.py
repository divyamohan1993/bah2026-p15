"""Master flare catalogue: association, de-dup, confidence, O(1) index.

Workstream 3 (ARCHITECTURE.md Appendix C). Intentionally empty so the
sub-package imports cleanly while its modules are built in parallel.
"""
