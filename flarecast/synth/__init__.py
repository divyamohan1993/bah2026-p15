"""Physics-based synthetic light-curve generator (offline keystone).

Workstream 1 (ARCHITECTURE.md Appendix C). Intentionally empty so the
sub-package imports cleanly while its modules are built in parallel.
"""
