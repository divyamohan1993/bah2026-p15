# Aditya FlareCast — minimal OFFLINE CORE dependencies.
#
# These three packages are all that the offline synthetic demo and the core
# test suite need. The entire pipeline (synth -> detect -> catalogue ->
# forecast -> dashboard) runs with ZERO network and ZERO credentials on just
# this core. Install with:  pip install -r requirements.txt
#
# Everything else is OPTIONAL and lives in pyproject.toml extras groups.
# Install an extra with, e.g.:  pip install -e ".[real]"   (or ml / api / dev)

numpy>=1.24
pandas>=2.0
scipy>=1.10

# ---------------------------------------------------------------------------
# Optional extras (NOT installed by this file) — see pyproject.toml:
#
#   [real] real data sources (live + archive):
#       astropy>=5.3  sunpy>=5.0  requests>=2.31  cdasws>=1.7  drms>=0.6
#
#   [ml]   machine-learning forecasters:
#       lightgbm>=4.0  scikit-learn>=1.3  onnxruntime>=1.16  torch>=2.0
#
#   [api]  serving layer:
#       fastapi>=0.110  uvicorn>=0.27
#
#   [dev]  development / CI tooling:
#       pytest>=7.4  ruff>=0.4
# ---------------------------------------------------------------------------
